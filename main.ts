// Função de multiplicar
function multiplicar(n1: number, n2: number): number {
    return n1 * n2;
}

// Função de saudação
function dizOla(nome: string): string {
    return `Olá ${nome}!`
}

console.log(multiplicar(4, 5));
console.log(dizOla('Bia'));